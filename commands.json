[
    {
        "type": "function",
        "function": {
            "name": "llt",
            "description": "llt is an alias for main() in the main.py file.",
            "parameters": {
                "type": "object",
                "properties": {
                    "ll": { 
                        "type": "string", 
                        "description": "The name of the ll file to load."
                    },
                    "file": { 
                        "type": "string", 
                        "description": "Read content from a file and include it in the ll."
                    },
                    "prompt": { 
                        "type": "string", 
                        "description": "Prompt string."
                    }
                }, 
                "required": ["ll", "file", "prompt"]
            }
        }
    }
]

